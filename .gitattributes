# Normalise line endings to LF in the repo; Windows checkouts get CRLF
* text=auto eol=lf

# Explicitly mark binary files so git never touches their line endings
*.pdf    binary
*.docx   binary
*.png    binary
*.jpg    binary
